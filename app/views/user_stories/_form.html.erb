<%= form_with(model: user_story, local: true) do |form| %>
  <% if user_story.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(user_story.errors.count, "error") %> prohibited this user_story from being saved:</h2>

      <ul>
      <% user_story.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :number %>
    <%= form.text_field :number, id: :user_story_number %>
  </div>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title, id: :user_story_title %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_area :description, id: :user_story_description %>
  </div>

  <div class="field">
    <%= form.label :points %>
    <%= form.text_field :points, id: :user_story_points %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
